# Copia este archivo como .env y reemplaza con la IP local de tu máquina
# (la misma red WiFi que tu celular). En Mac/Linux: ifconfig | grep "inet "
# En Windows: ipconfig | findstr IPv4
EXPO_PUBLIC_API_URL=http://192.168.1.X:8000/api
